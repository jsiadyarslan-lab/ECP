# cases/ — RESERVED (empty at R0)

This directory is reserved for **future public case representations**.

At repository foundation (R0) it MUST remain empty: no scientific evaluation case,
no registered case, no development case beyond the clearly marked illustrations that
live under `examples/`, and no ground truth of any kind.

Only this `README.md` may be present. The boundary scanner
(`python tools/ecp_cli.py boundary-scan`) and the test suite enforce this rule.

Case authoring, review and registration are later, separately authorized phases; they
do not exist in this repository yet.
